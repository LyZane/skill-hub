# psd-guide-slicer 示例提示词

复制下面任一场景的提示词发给 agent，替换 `<尖括号>` 占位符即可使用。如果 agent 没有自动调用本技能，在提示词开头加上「使用 psd-guide-slicer 技能：」即可显式指定。

## 场景 1｜按 PSD 参考线切片（最常用）

> 请按 PSD 参考线切分这张详情长图：PSD 文件 <PSD/PSB路径>，配套导出长图 <长图路径>。

## 场景 2｜无参考线 → 按固定高度等分

> 这份详情长图 <长图路径> 的源文件里没有参考线，请按每张 <高度，如 1500> 像素等分切片。
