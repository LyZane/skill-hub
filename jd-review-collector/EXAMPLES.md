# jd-review-collector 示例提示词

复制下面的提示词发给 agent，替换 `<尖括号>` 占位符即可使用。如果 agent 没有自动调用本技能，在提示词开头加上「使用 jd-review-collector 技能：」即可显式指定。采集依赖浏览器中已登录的京东会话，请先确认登录状态。

## 场景 1｜单商品评论采集 + 口碑报告

> 请采集这个京东商品的带图评论并做口碑分析，输出 HTML 报告：<商品链接>。我的浏览器已登录京东。
